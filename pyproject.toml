[project]
name = "technology-evolution-engine"
version = "0.1.0"
description = "Technology Evolution Engine"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"