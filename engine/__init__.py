"""engine — the mechanism-to-discovery engine (Phase 1-17).

This package implements the CEO directive's discovery engine. It builds
on the hardened substrate (discovery_infrastructure.discovery_substrate)
and adds real scientific reasoning machinery.

Rule 0: NONE of this touches the frozen Gate 2 experiment. Development
uses engine/dev_fixtures.py only.
"""
